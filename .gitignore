# Beads / Dolt (from bd init + survives across clones)
.dolt/
*.db
.beads-credential-key
.beads/embeddeddolt/

# Experiment workstream notes — local-only, never committed.
experiments/

# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
.coverage
htmlcov/

# Mutmut mutation-testing artifacts (trip-concierge-ht5)
# `mutants/` is a generated mirror of source; .mutmut-cache stores survival
# state. Run results go to experiments/ (also gitignored).
backend/mutants/
backend/.mutmut-cache
.mutmut-cache

# Node
node_modules/
.next/
.turbo/
dist/
build/
.pnpm-store/

# Env (commit .env.example only)
.env
.env.local
.env.*.local
!.env.example

# IDE / OS
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db
